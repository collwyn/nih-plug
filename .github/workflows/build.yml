name: Automated Builds

on:
  push:
    branches:
      - '**'
    tags:
      - '*'
  pull_request:
    branches:
      - master
  workflow_dispatch:

defaults:
  run:
    shell: bash

jobs:
  package:
    strategy:
      matrix:
        include:
          - { name: ubuntu-22.04,    os: ubuntu-22.04,   cross-target: '' }
          - { name: macos-universal, os: macos-latest,   cross-target: x86_64-apple-darwin }
          - { name: windows,         os: windows-latest, cross-target: '' }

    name: Package plugin binaries
    runs-on: ${{ matrix.os }}

    steps:
      # -----------------------------
      # CHECKOUT + SUBMODULES + FULL HISTORY
      # -----------------------------
      - uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Ensure submodules are initialized
        run: git submodule update --init --recursive

      - name: Fetch all git history
        run: git fetch --force --prune --tags --unshallow || true

      # -----------------------------
      # UBUNTU DEPENDENCIES
      # -----------------------------
      - name: Install Linux dependencies
        if: startsWith(matrix.os, 'ubuntu')
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libasound2-dev libgl-dev libjack-dev libx11-xcb-dev \
            libxcb1-dev libxcb-dri2-0-dev libxcb-icccm4-dev \
            libxcursor-dev libxkbcommon-dev libxcb-shape0-dev \
            libxcb-xfixes0-dev

      # -----------------------------
      # CACHE
      # -----------------------------
      - uses: actions/cache@v4
        if: startsWith(matrix.os, 'windows')
        with:
          path: |
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
          key: ${{ matrix.name }}-${{ matrix.cross-target }}

      - uses: actions/cache@v4
        if: "!startsWith(matrix.os, 'windows')"
        with:
          path: |
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ matrix.name }}-${{ matrix.cross-target }}

      # -----------------------------
      # RUST TOOLCHAIN
      # -----------------------------
      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@nightly
        with:
          targets: ${{ matrix.cross-target }}

      # -----------------------------
      # BUILD + PACKAGE
      # -----------------------------
      - name: Package all targets from bundler.toml
        run: |
          package_args=()
          for package in $(cargo xtask known-packages); do
            package_args+=("-p" "$package")
          done

          runner_name=${{ matrix.name }}

          if [[ $runner_name = 'macos-universal' ]]; then
            export MACOSX_DEPLOYMENT_TARGET=10.13
            cargo xtask bundle-universal "${package_args[@]}" --release
          else
            cross_target=${{ matrix.cross-target }}
            if [[ -n $cross_target ]]; then
              package_args+=("--target" "$cross_target")
            fi
            cargo xtask bundle "${package_args[@]}" --release
          fi

      # -----------------------------
      # ARCHIVE + ARTIFACT
      # -----------------------------
      - name: Determine build archive name
        run: |
          echo "ARCHIVE_NAME=nih-plugs-$(date -u +"%Y-%m-%d-%H%M%S")-${{ matrix.name }}" >> "$GITHUB_ENV"

      - name: Move packaged plugins into archive directory
        run: |
          mkdir -p "$ARCHIVE_NAME/$ARCHIVE_NAME"
          mv target/bundled/* "$ARCHIVE_NAME/$ARCHIVE_NAME"

      - name: Add OS-specific README
        run: cp ".github/workflows/readme-${{ runner.os }}.txt" "$ARCHIVE_NAME/$ARCHIVE_NAME/README.txt"

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ env.ARCHIVE_NAME }}
          path: ${{ env.ARCHIVE_NAME }}
